{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Campaign",
  "type": "object",
  "description": "A campaign is a story that uses a system codex as its ruleset.",
  "required": ["name"],
  "properties": {
    "name": {
      "type": "string",
      "description": "The full name of the campaign."
    },
    "system": {
      "type": "string",
      "description": "The short_name of the system codex this campaign is based on.",
      "pattern": "[a-zA-Z_-]+"
    },
    "version": {
      "type": "string",
      "description": "The version of the campaign."
    },
    "description": {
      "type": "string",
      "description": "A short description of the campaign."
    },
    "icon": {
      "type": "string",
      "description": "Icon filename of the campaign, relative to this file."
    }
  }
}
